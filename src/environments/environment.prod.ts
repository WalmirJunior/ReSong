export const environment = {
  production: true,
  lastFmApiKey: '', // Deixe isso vazio no código público ou configure via variável de ambiente no servidor.
  spotifyClientId: '', // Deixe isso vazio no código público ou configure via variável de ambiente no servidor.
  spotifyClientSecret: '', // Deixe isso vazio no código público ou configure via variável de ambiente no servidor.
  spotifyApiKey: '', // Deixe isso vazio no código público ou configure via variável de ambiente no servidor.
};
